Build a retrieval-augmented generation system from scratch that actually handles the hard parts: chunking strategy, hybrid search (dense + BM25), reranking, and handling contradictory sources. 
The naive version is a weekend tutorial; the advanced version is making retrieval quality measurable and tuning it. 
Pair it with an eval harness so you can prove improvements rather than vibe-check them.



**New changes Should be Updated here**
